/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['pixabay.com', 'example.com'], // Add any external image host here
    },
  };
  
  export default nextConfig;
  